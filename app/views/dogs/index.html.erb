<section class="back-img">
  <div class="container">
    <h1 class="text-center">Pets</h1>

    <% if policy(Dog.new).create? %>
      <%= link_to "New Dog", new_dog_path, class: "btn btn-primary my-3 d-block mx-auto" %>
    <% end %>

    <%= render "dogs/filters" %>

    <%= render "dogs/dogs_collection", dogs: @dogs %>
  </div>
</section>
